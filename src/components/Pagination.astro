---
interface Props {
  currentPage: number;
  totalPages: number;
  prevUrl?: string;
  nextUrl?: string;
}

const { currentPage, totalPages, prevUrl, nextUrl } = Astro.props;
---

<nav
  class="flex justify-between items-center mt-8 pt-4 border-t border-gray-200"
>
  <div>
    {
      prevUrl && (
        <a href={prevUrl} class="text-blue-600 hover:underline">
          ← Newer
        </a>
      )
    }
  </div>
  <span class="text-gray-600">Page {currentPage} of {totalPages}</span>
  <div>
    {
      nextUrl && (
        <a href={nextUrl} class="text-blue-600 hover:underline">
          Older →
        </a>
      )
    }
  </div>
</nav>

